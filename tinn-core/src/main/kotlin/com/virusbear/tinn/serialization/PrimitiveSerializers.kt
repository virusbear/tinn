package com.virusbear.tinn.serialization
/*
import com.virusbear.tinn.registry.Registry

val BooleanSerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.boolean }
)

val ByteSerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.byte }
)

val IntSerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.int }
)

val LongSerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.long }
)

val DoubleSerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.double }
)

val StringSerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.string }
)

val ByteArraySerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.byteArray }
)

val IntArraySerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.intArray }
)

val LongArraySerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.longArray }
)

val DoubleArraySerializer = Serializer(
    serializer = { it.encoded },
    deserializer = { it.doubleArray }
)

internal fun Registry<Serializer<*>>.registerPrimitiveSerializers() {
    register("tinn:boolean", BooleanSerializer)
    register("tinn:byte", ByteSerializer)
    register("tinn:int", IntSerializer)
    register("tinn:long", LongSerializer)
    register("tinn:double", DoubleSerializer)
    register("tinn:string", StringSerializer)
    register("tinn:bytearray", ByteArraySerializer)
    register("tinn:intarray", IntArraySerializer)
    register("tinn:longarray", LongArraySerializer)
    register("tinn:doublearray", DoubleArraySerializer)
}*/